folder should contain symbolic links to raw datasets.

If preliminary transformations ae needed, corresponding code should also be stored here and intermediate data stored in ../temp/, which is not hosted on the repo.